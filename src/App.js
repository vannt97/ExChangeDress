import React from "react";
import { useSelector } from "react-redux";
import ExChangeDress from "./ExChangeDress/ExChangeDress";
function App() {
  return (
    <div className="App">
      <ExChangeDress />
    </div>
  );
}

export default App;
